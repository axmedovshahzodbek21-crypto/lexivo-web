'use client';
export default function ClassLeaderboardPage() {
  return (
    <div className="flex flex-col items-center justify-center min-h-screen gap-4 text-center p-8">
      <span className="text-5xl">🏆</span>
      <h1 className="text-xl font-bold text-[var(--text)]">Class Leaderboard</h1>
      <p className="text-sm text-[var(--text-muted)]">Podium + leaderboard with XP — coming in Phase 2</p>
    </div>
  );
}
